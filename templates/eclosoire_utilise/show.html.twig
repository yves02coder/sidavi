{% extends 'base.html.twig' %}

{% block title %}EclosoireUtilise{% endblock %}

{% block body %}
    <h1>EclosoireUtilise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdEclosoireUtilise</th>
                <td>{{ eclosoire_utilise.idEclosoireUtilise }}</td>
            </tr>
            <tr>
                <th>QteOeuf</th>
                <td>{{ eclosoire_utilise.qteOeuf }}</td>
            </tr>
            <tr>
                <th>NumCaisse</th>
                <td>{{ eclosoire_utilise.numCaisse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('eclosoire_utilise_index') }}">back to list</a>

    <a href="{{ path('eclosoire_utilise_edit', {'idEclosoireUtilise': eclosoire_utilise.idEclosoireUtilise}) }}">edit</a>

    {{ include('eclosoire_utilise/_delete_form.html.twig') }}
{% endblock %}
