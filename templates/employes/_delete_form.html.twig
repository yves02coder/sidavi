<form method="post" action="{{ path('employes_delete', {'idEmploye': employe.idEmploye}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.idEmploye) }}">
    <button class="btn">Delete</button>
</form>
