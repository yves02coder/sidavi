<form method="post" action="{{ path('maintenance_delete', {'idMaintenance': maintenance.idMaintenance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance.idMaintenance) }}">
    <button class="btn">Delete</button>
</form>
