<form method="post" action="{{ path('suivi_delete', {'idSuivi': suivi.idSuivi}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi.idSuivi) }}">
    <button class="btn">Delete</button>
</form>
